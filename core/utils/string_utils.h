#pragma once

namespace uco
{
    /**
     * Converts a character to its hexadecimal value.
     * @param c The character to convert.
     * @return The hexadecimal value of the character, or -1 if invalid.
     */
    int CharToHex(char c);
}
